[core]
editor = nvim
excludesfile = ~/.gitignore_global

[include]
path = ~/.gitconfig_local
path = /home/tallen/.gofork_config

[push]
default = simple

[user]
name = Tommy Allen
email = tommy@esdf.io

[github]
user = tweekmonster

[alias]
pignore = "!gi() { curl -L -s https://www.gitignore.io/api/$@ | tail -n+3 ;}; gi"
fetch-upstream = fetch -p --tags upstream
log-upstream = log upstream/master..
